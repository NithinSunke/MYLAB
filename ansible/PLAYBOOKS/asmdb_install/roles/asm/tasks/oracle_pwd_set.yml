---
- name: Set oracle user password
  user:
     name: oracle
     password: "{{ ORACLE_PASSWORD  | password_hash('sha512') }}"

- name: Ensure /u01 exists
  file:
    path: /u01
    state: directory
    owner: oracle
    group: oinstall
    mode: "0775"
    recurse: yes