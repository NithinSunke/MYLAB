LISTENER =
  (DESCRIPTION_LIST =
    (DESCRIPTION =
      (ADDRESS = (PROTOCOL = TCP)(HOST = {{ standby_host }})(PORT = {{ listener_port }}))
      (ADDRESS = (PROTOCOL = IPC)(KEY = EXTPROC1))
    )
  )

SID_LIST_LISTENER =
  (SID_LIST =
    (SID_DESC =
      (GLOBAL_DBNAME = {{ standby_db }})
      (ORACLE_HOME = {{ oracle_home }})
      (SID_NAME = {{ standby_db }})
    )
  )
