- name: Change hostname
  hostname:
    name: "{{ new_hostname }}"

- name: Add new IP address and hostname with domain name to /etc/hosts
  lineinfile:
    path: /etc/hosts
    line: "{{ new_ip }} {{ new_hostname }}.scslabs.com {{ new_hostname }}"
    state: present
