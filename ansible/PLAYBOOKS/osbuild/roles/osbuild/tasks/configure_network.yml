
- name: Configure new IP address
  copy:
    dest: /etc/sysconfig/network-scripts/ifcfg-ens18
    content: |
      DEVICE=ens18
      BOOTPROTO=static
      ONBOOT=yes
      IPADDR={{ new_ip }}
      NETMASK={{ new_netmask }}
      GATEWAY={{ new_gateway }}
      DNS1={{ new_dns }}