#- name: Create a group
#  group:
#    name: "admin_group"
#    state: present

#- name: Give the group sudo privileges without a password
#  lineinfile:
#    path: /etc/sudoers
#    state: present
#    regexp: '^%admin_group'
#    line: '%admin_group ALL=(ALL) NOPASSWD: ALL'
#    validate: 'visudo -cf %s'

#- name: Create a user and assign to the group
#  user:
#    name: "nsunke"
#    groups: "admin_group"
#    state: present
#    shell: /bin/bash

#- name: Set nsunke user password
#  user:
#     name: nsunke
#     password: "{{ NSUNKE_PASSWORD  | password_hash('sha512') }}"

- name: Set root password
  user:
    name: root
    password: "{{ ROOT_PASSWORD | password_hash('sha512') }}"

